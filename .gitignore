node_modules
.next
.vercel
*.tsbuildinfo
.env*
!.env.example
npm-debug.log*
*.zip
.DS_Store

# Restore points for destructive corpus operations. Local only: they contain the
# whole post text and exist for the minutes after a bulk delete, not for review.
backups/
